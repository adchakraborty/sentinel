# GitHub Models API token (requires models:read scope)
# SENTINEL reads this from your system environment variables.
# If not set as a system env var, you can set it here:
GITHUB_TOKEN=ghp_your_token_here
